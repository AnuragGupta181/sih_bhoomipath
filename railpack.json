{
  "buildCommand": "cd backend && python -m pip install -r requirements.txt",
  "startCommand": "cd backend && gunicorn server:app --bind 0.0.0.0:$PORT",
  "watchPaths": ["backend"],
  "env": {
    "RAILPACK_PYTHON_VERSION": "3.11.13"
  }
}
